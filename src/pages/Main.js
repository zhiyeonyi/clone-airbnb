import React from "react";
import MainTop from "../components/MainTop";

const Main = (props) => {
  return (
    <React.Fragment>
      <MainTop />
    </React.Fragment>
  );
};

export default Main;
