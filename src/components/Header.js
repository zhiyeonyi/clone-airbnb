import React from "react";

import { Grid, Text, Button, Input } from "../elements";

const Header = (props) => {
  return (
    <React.Fragment>
      <h1>헤더</h1>
    </React.Fragment>
  );
};

export default Header;
